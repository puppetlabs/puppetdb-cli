#[macro_use] extern crate hyper;
extern crate docopt;
extern crate serde;

use hyper::Client;
use hyper::header::{Connection,Headers};
use docopt::Docopt;


fn main() {
    println!("Hello, world!");
}
