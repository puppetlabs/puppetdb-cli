language: rust
matrix:
    fast_finish: true
    include:
        - rust: stable

sudo: false

cache:
    apt: true
    directories:
        - target/debug/deps
        - target/debug/build

script:
  - cargo build --verbose
  - PATH=$PATH:./target/debug puppet-pdb --version